{
  "name": "church-website-proxy",
  "version": "1.0.0",
  "description": "Local proxy to add CORS headers when developing the Rover Team form",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  }
}
